name: beanheads
description: A customizable cartoon avatar generator that produces SVG strings. Dart port of the BeanHeads library.
version: 0.2.0
repository: https://github.com/dartfika/beanheads-dart
homepage: https://github.com/dartfika/beanheads-dart
topics:
  - avatar
  - svg
  - cartoon
  - generator

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
