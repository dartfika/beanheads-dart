/// All option enums for configuring a BeanHead avatar.
library;

/// Skin tone options for the avatar.
enum SkinTone { light, yellow, brown, dark, red, black }

/// Eye style options.
enum Eyes {
  normal,
  leftTwitch,
  happy,
  content,
  squint,
  simple,
  dizzy,
  wink,
  heart,
}

/// Eyebrow style options.
enum Eyebrows { raised, leftLowered, serious, angry, concerned }

/// Mouth style options.
enum Mouth { grin, sad, openSmile, lips, open, serious, tongue }

/// Hair style options.
enum Hair { none, long, bun, short, pixie, balding, buzz, afro, bob }

/// Facial hair style options.
enum FacialHair { none, stubble, mediumBeard }

/// Clothing style options.
enum Clothing { naked, shirt, dressShirt, vneck, tankTop, dress }

/// Accessory options.
enum Accessory { none, roundGlasses, tinyGlasses, shades }

/// Graphic options for clothing.
enum ClothingGraphic { none, redwood, gatsby, vue, react, graphQL }

/// Hat options.
enum Hat { none, beanie, turban }

/// Body shape options.
enum Body { chest, breasts }

/// Hair color options.
enum HairColor { blonde, orange, black, white, brown, blue, pink }

/// Clothing color options.
enum ClothingColor { white, blue, black, green, red }

/// Background circle color options.
enum CircleColor { blue }

/// Lip color options.
enum LipColor { red, purple, pink, turquoise, green }

/// Hat color options.
enum HatColor { white, blue, black, green, red }

/// Face mask color options.
enum FaceMaskColor { white, blue, black, green, red }
